{
  "private": true,
  "workspaces": [
    "web",
    "service"
  ],
  "scripts": {
    "getAirports": "yarn workspace service run ts-node -r tsconfig-paths/register ./getAirports.ts",
    "getFares": "yarn workspace service run ts-node -r tsconfig-paths/register ./getFares.ts",
    "web": "yarn workspace web run next dev"
  }
}